import FinaleClient from "@/components/FinaleClient";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Happy Birthday, Kim 🎂",
};

export default function FinalePage() {
  return <FinaleClient />;
}
